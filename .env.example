# =============================================================================
# OrbitLLM Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Your Azure OpenAI endpoint URL
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# Your Azure OpenAI API key
AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# Model deployment name (default: gpt-5-chat)
# Common values: gpt-4o-mini, gpt-5-chat, gpt-4o
MODEL_NAME=gpt-5-chat

# -----------------------------------------------------------------------------
# Admin API Configuration (REQUIRED for admin endpoints)
# -----------------------------------------------------------------------------
# API key for admin endpoints (/admin/*)
# Generate a secure random key: openssl rand -hex 32
ADMIN_API_KEY=your-secure-admin-api-key-here

# -----------------------------------------------------------------------------
# Azure Blob Storage (OPTIONAL - for production persistence)
# -----------------------------------------------------------------------------
# If not set, knowledge base files will be stored locally
# Recommended for production to persist data across container restarts
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...

# Azure Blob container name for knowledge base files
# AZURE_STORAGE_CONTAINER_NAME=knowledge-base

# -----------------------------------------------------------------------------
# Azure Document Intelligence (REQUIRED for PDF upload feature)
# -----------------------------------------------------------------------------
# Required if you want to enable PDF to Markdown conversion
# Get from Azure Portal > Document Intelligence resource
# AZURE_DOCINTEL_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
# AZURE_DOCINTEL_API_KEY=your-document-intelligence-api-key

# -----------------------------------------------------------------------------
# Traefik Configuration (for docker-compose.yml with SSL)
# -----------------------------------------------------------------------------
# Email for Let's Encrypt SSL certificates
# ACME_EMAIL=your-email@example.com
